#!/usr/bin/env node

/**
 * Manual Redesign Test — DEPRECATED
 *
 * This simulated the retired single-call redesign (submit_redesign JSON via
 * the prompt-builder's Unified Designer prompt). That pipeline was replaced
 * by the mockup pipeline (Mockup Designer → Mockup Critic → React Engineer)
 * in design-agents.js.
 */

console.error(
  'test-redesign-manual is superseded by the mockup pipeline — run scripts/run-pipeline.js'
)
process.exit(1)
