#!/usr/bin/env node

/**
 * Generate Redesign — DEPRECATED
 *
 * This local-dev path built the retired Unified Designer prompt via the
 * prompt-builder. The Unified Designer was replaced by the mockup pipeline
 * (Mockup Designer → Mockup Critic → React Engineer) in design-agents.js.
 */

console.error('generate-redesign is superseded by the mockup pipeline — run scripts/run-pipeline.js')
process.exit(1)
