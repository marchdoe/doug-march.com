# Designer Pipeline Measurements — 2026-04-13 onward

## Purpose
Track designer prompt size and per-build cost as we move through the Phase 0 → Phase 3 plan. Each row is captured at the END of a phase, after the phase's commits are in place.

## Columns
- **Phase** — which phase produced this state
- **unified-designer.md bytes** — primary source file size
- **prompt-builder.js bytes** — assembler size (declining across phases as it's thinned)
- **Notes**

## Measurements

| Phase | unified-designer.md bytes | prompt-builder.js bytes | Notes |
|------|---------------------------|-------------------------|-------|
| Pre-Phase-0 (historical) | 9443 | ~9600 | Two drifted prompts — production from markdown, local dev from embedded template literal |
| Post-Phase-0 | 14202 | 7608 | Single source; markdown holds all prompt content, assembler reads it via readFileSync. Byte-identity test enforced. |
| Phase-1 baseline | 14202 | 7608 | Identical to Post-Phase-0 — Phase 1 starts here. 5% growth budget = +710 bytes ceiling on unified-designer.md. |

Raw measurement (`wc -c`):

```
   14202 scripts/prompts/unified-designer.md
    7608 scripts/utils/prompt-builder.js
   21810 total
```

## Per-build cost notes (Phase 0)

The unified prompt grew to approximately 14202 characters vs 9443 pre-unification. Estimated input-token impact: +1190 tokens per production build (production path was already reading unified-designer.md). Local-dev input tokens shift to match — previously ran on the smaller embedded template, now uses the same unified content.

This cost is a one-time unification expense. Phase 1's 5% growth budget is measured against this Post-Phase-0 baseline, not against pre-Phase-0.

## Phase 1 baseline (entry point)

Starting file sizes for Phase 1:
- `scripts/prompts/unified-designer.md`: **14202 bytes**
- 5% growth ceiling: +710 bytes → **≤14912 bytes** after Phase 1 (Task 1.2 + 1.3 combined).

Per-build token/elapsed time: not measured in-session (a real LLM build would consume significant session tokens). Will be captured as a delta at Task 1.4 via one full local-dev build after Phase 1 content ships.
