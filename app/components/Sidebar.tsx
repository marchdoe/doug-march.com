// Navigation lives inline within each page composition.
// This component is intentionally empty — the Split layout
// embeds navigation in the left panel, about/work pages
// handle their own top bars.
export function Sidebar() {
  return null
}